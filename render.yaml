services:
  - type: web
    name: mini-case-tracker-api
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm ci || npm install && npm run build
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
